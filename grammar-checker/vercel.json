{
  "version": 2,
  "buildCommand": "npm install --force && npm run build:vercel",
  "outputDirectory": "dist",
  "installCommand": "npm install --force",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}