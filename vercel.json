{
  "version": 2,
  "buildCommand": "cd grammar-checker && npm install && npm run build:vercel",
  "outputDirectory": "grammar-checker/dist",
  "framework": "vite",
  "installCommand": "npm install --force",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}